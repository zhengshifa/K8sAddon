---
# Source: argo-cd/charts/redis-ha/templates/tests/test-redis-ha-pod.yaml
apiVersion: v1
kind: Pod
metadata:
  name: argo-cd-redis-ha-service-test
  namespace: "argo-cd"
  labels:
    app: redis-ha
    heritage: "Helm"
    release: "argo-cd"
    chart: redis-ha-4.27.6
  annotations:
    "helm.sh/hook": test-success
spec:
  nodeSelector:
    {}
  tolerations:
    []
  containers:
  - name: "argo-cd-service-test"
    image: public.ecr.aws/docker/library/redis:7.2.4-alpine
    command:
      - sh
      - -c
      - redis-cli -h argo-cd-redis-ha-haproxy -p 6379 info server
    resources: 
      {}
    securityContext: 
      allowPrivilegeEscalation: false
      capabilities:
        drop:
        - ALL
      readOnlyRootFilesystem: true
      runAsNonRoot: true
      runAsUser: 1000
      seccompProfile:
        type: RuntimeDefault
  restartPolicy: Never
